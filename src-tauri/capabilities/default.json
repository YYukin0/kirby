{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. Our file read/write are app-defined commands, so no extra plugin permissions are needed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "autostart:default",
    "window-state:default",
    "dialog:allow-open"
  ]
}
