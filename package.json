{
  "name": "typewriter-plan",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Tauri desktop widget: paste a study plan, get a live checklist, auto-save to your Obsidian vault.",
  "scripts": {
    "build:ui": "tsc",
    "watch:ui": "tsc --watch",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
