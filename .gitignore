node_modules/
ui/main.js
ui/plan.js
src-tauri/target/
src-tauri/gen/
icon-source.png
.DS_Store
# sandbox/ is scratch, except the browser harness — it carries __fxSoak, the
# memory regression probe for the fx-layer leak. Exclude the contents rather
# than the directory, or the negation below can't re-include anything.
sandbox/*
!sandbox/harness.html
